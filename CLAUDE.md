# CLAUDE.md

Context and conventions for AI-assisted development on this repo.

---

## What this repo is

`win_dell` is a fully unattended Windows 10/11 post-install automation framework for Dell hardware. A single `irm | iex` one-liner triggers it; everything after — power settings, debloat, registry tweaks, display scale, app installs, Tailscale registration, Windows Update, and cleanup — runs automatically across reboots until complete.

Target hardware: Dell laptops/desktops, Windows 10 21H2+ and Windows 11 22H2+.
**All code must run on Windows PowerShell 5.1.** PowerShell 7 is not assumed.

---

## Repo layout

```
win_dell/
├── install.ps1              irm|iex entry point
├── uninstall.ps1            removes all WinDeploy components (force-kills first)
├── reinstall.ps1            unattended uninstall + fresh install in one step
├── bootstrap.ps1            sets up tasks, state, launches monitor + orchestrator
├── lint.ps1                 PSScriptAnalyzer + custom PS51/task/path checks
├── manifest.json            auto-generated by CI — DO NOT EDIT BY HAND
├── manifest.sig             GPG detached signature of manifest.json
│
├── core/
│   ├── Config.psm1          $WD.* constants — single source of truth
│   ├── State.psm1           state.json read/write, reboot counter, stage tracking
│   ├── Logging.psm1         Write-LogInfo/Success/Warning/Error + file output
│   ├── Resilience.psm1      task self-healing, dir ACLs, state quarantine, watchdog
│   ├── Orchestrator.ps1     master controller — runs stages in order
│   ├── Monitor.ps1          WPF progress window, Tailscale QR, error panel
│   ├── Notify.ps1           tray notification on completion, self-removes
│   ├── Diagnostic.ps1       standalone diagnostic tool
│   ├── PowerSettings.ps1    stage 1: power/sleep/display settings
│   ├── Debloat.ps1          stage 2: remove bloatware
│   ├── WinTweaks.ps1        stage 3: tweaks (see below)
│   ├── AppInstall.ps1       stages 4-5: Dell SupportAssist, Power Manager
│   ├── Tailscale.ps1        stage 6: install + QR registration
│   ├── WindowsUpdate.ps1    stage 7: Windows Update (last — multiple reboots)
│   └── Cleanup.ps1          stage 8: remove tasks, disable auto-logon, reboot
│
├── config/
│   ├── settings.json        all deployment config — edit this, not the scripts
│   └── winutil-preset.json  WinUtil tweak IDs
│
├── data/
│   └── bloatware.json       safe/optional app removal lists
│
├── docs/
│   └── gpg-setup.md         generate GPG key pair for manifest signing
│
└── .github/workflows/
    └── ci.yml               lint → validate-tasks → sign manifest (sequential)
```

---

## One-liner

```powershell
irm "https://raw.githubusercontent.com/karolperkowski/win_dell/main/install.ps1" | iex
```

Re-running updates scripts and preserves state. The monitor window launches immediately — no need to wait for a reboot.

---

## PowerShell 5.1 rules — enforce on every change

| Forbidden | PS 5.1 alternative |
|---|---|
| `$x ?? $y` | `if ($x) { $x } else { $y }` |
| `$x?.Prop` | `if ($x) { $x.Prop }` |
| `??=` | `if (-not $x) { $x = $y }` |
| `[array].Count` on pipeline result | `@($result).Count` |
| `ConvertFrom-Json -AsHashtable` | Gate behind `$PSVersionTable.PSVersion.Major -ge 6`, use `ConvertTo-Hashtable` shim on 5.1 |
| `Get-WindowsUpdate -AcceptAll` | `-AcceptAll` only valid on `Install-WindowsUpdate` |
| `trap { continue }` to skip failed assignment | Use `try/catch` — `continue` resumes after the failed statement, not at the catch |
| `Add-Content` (any usage) | **Banned.** Writes UTF-16LE in PS 5.1, blocks concurrent readers. Use `[System.IO.File]::AppendAllText(path, text, UTF8)`. Lint rule `IO-BannedAddContent` enforces this. |
| `CharacterSpacing` in WPF XAML | Silverlight-only — not available in WPF, remove it |
| `-LogonType Interactive` with `-GroupId` | Incompatible parameter set — omit `-LogonType` for group-based principals |
| `New-ScheduledTaskPrincipal -GroupId` + `-LogonType` | Invalid combination — use one or the other |
| `(if ($x) { $y } else { $z })` as argument | PS 5.1 `if` is a statement, not expression — assign to variable first |
| `*>> $file` in launcher scripts | Writes UTF-16LE on PS 5.1 — use `*>&1 \| Out-File -Append -Encoding UTF8` |

---

## Shared constants — always use $WD.*

`core/Config.psm1` exports a `Get-WDConfig` function (not a variable). Every script that needs a path or task name imports this:

```powershell
Import-Module (Join-Path $PSScriptRoot 'Config.psm1') -DisableNameChecking -Force
$WD = Get-WDConfig
```

**Why a function, not a variable:** PS 5.1 + `Set-StrictMode -Version Latest` does not reliably resolve module-exported variables (`Export-ModuleMember -Variable`) across child process boundaries. Functions always work.

Key values:

| Constant | Value |
|---|---|
| `$WD.DeployRoot` | `C:\ProgramData\WinDeploy` |
| `$WD.StateFile` | `C:\ProgramData\WinDeploy\state.json` |
| `$WD.LogDir` | `C:\ProgramData\WinDeploy\Logs` |
| `$WD.TailscaleJson` | `C:\ProgramData\WinDeploy\tailscale.json` |
| `$WD.TaskResume/Monitor/Notify/Safety/Watchdog` | task name strings |
| `$WD.StageOrder` | ordered array of stage names |
| `$WD.StageLabels` | display name hashtable |
| `$WD.RebootAllowedStages` | which stages may trigger a reboot |

**Never hardcode `C:\ProgramData\WinDeploy` in a stage script.**
**Never duplicate the stage list — it lives only in `Config.psm1`.**

Scripts that run before Config loads (bootstrap, install, uninstall, Resilience early log, Monitor pre-WPF) are permitted to hardcode the path as a fallback.

---

## Stage pipeline order

```
PowerSettings → Debloat → WinTweaks → InstallDellSupportAssist →
InstallDellPowerManager → InstallTailscale → WindowsUpdate → Cleanup
```

WindowsUpdate runs **last** because it takes the longest and requires multiple reboots. All useful config work is done first so reboots only happen during updates.

---

## WinTweaks stage — applied tweaks

- Dark theme (HKLM + HKCU + Default user hive)
- **Display scale → 100% (96 DPI)** for current user and default hive
- NumLock on (current + default hive)
- Bing search removed from Start
- Verbose login enabled
- Timezone set to Eastern Standard Time
- Telemetry disabled (AllowTelemetry = 0)
- Activity history disabled
- Location tracking denied
- File extensions always visible
- Hidden files shown
- Xbox Game DVR / Game Bar disabled
- Fast startup disabled
- Widgets disabled (Windows 11)
- Clock seconds shown
- Chrome installed via winget
- WinUtil preset applied from `config/winutil-preset.json`

---

## Adding a new stage

1. Create `core/YourStage.ps1` using an existing stage as template.
2. Must return one of:
   ```powershell
   @{ Status = 'Complete';       Message = 'Human readable.' }
   @{ Status = 'RebootRequired'; Message = 'Why reboot needed.' }
   @{ Status = 'Failed';         Message = $_.Exception.Message }
   ```
3. Add stage name to `$WD.StageOrder` in `Config.psm1` at correct position.
4. Add label to `$WD.StageLabels` in `Config.psm1`.
5. Add to `$Script:STAGE_SCRIPTS` in `Orchestrator.ps1`.
6. Add config entry in `config/settings.json` under `"Stages"`.
7. If stage may reboot, add to `$WD.RebootAllowedStages` in `Config.psm1`.

Stages must **not** call `Restart-Computer` directly — return `RebootRequired` and let the orchestrator handle it.

---

## Scheduled tasks

| Task | Runs as | Launch | Removed by |
|---|---|---|---|
| `WinDeploy-Resume` | SYSTEM | Via `launch_resume.ps1` → `task_resume.log` | Cleanup.ps1 |
| `WinDeploy-Monitor` | Users | Direct (no launcher) | Cleanup.ps1 |
| `WinDeploy-Notify` | Users | Via `launch_notify.ps1` → `task_notify.log` | Notify.ps1 (self) |
| `WinDeploy-AutoLogonSafety` | SYSTEM | Direct | Self-deletes after 6h |
| `WinDeploy-Watchdog` | SYSTEM | Direct | Never (permanent) |

Monitor runs directly (no launcher) because child processes cannot show WPF windows on the interactive desktop. Resume and Notify use launchers to capture stdout/stderr.

All tasks are registered by `Resilience.psm1::Assert-ScheduledTasks` — the single registration point. Bootstrap and the orchestrator both call this on startup.

**Key rules:**
- `-LogonType Interactive` is incompatible with `-GroupId` — omit it
- Safety task trigger: `-Once -At future` with `EndBoundary` set explicitly
- Watchdog trigger: `-RepetitionInterval` requires `-RepetitionDuration (New-TimeSpan -Days 9999)` to avoid missing `EndBoundary` XML error

---

## Monitor window

- Launched by bootstrap.ps1 immediately (no reboot needed to see it)
- Launched by `WinDeploy-Monitor` task on every subsequent logon
- Polls `state.json` every 3 seconds via a `DispatcherTimer`
- Shows: current stage, stage list with timestamps, progress bar, elapsed time, reboot count
- Tailscale QR panel appears when `InstallTailscale` is the current stage
- Error panel (red, bottom of window) shows on any Update-UI error — window never closes on error
- `WindowStartupLocation = CenterScreen` + bounds clamp in `Add_Loaded` keeps it on-screen
- Auto-closes 60 seconds after `DeployComplete = true`; a "Close" button appears on completion for manual dismiss

**Critical:** `$PSScriptRoot` is empty when Monitor is launched via `Start-Process -ArgumentList`. Use `$MyInvocation.MyCommand.Path` fallback to locate `Config.psm1`.

---

## Logging

Use `Write-LogInfo`, `Write-LogSuccess`, `Write-LogWarning`, `Write-LogError` from `Logging.psm1`. Call `Initialize-Logger -Stage 'StageName'` at the top of every stage and `Close-Logger -FinalStatus 'SUCCESS'|'FAILED'` before every return.

**Never use `Write-Host` in stage scripts — it bypasses the log file.**
**Never use `Add-Content` for log writes — use `[System.IO.File]::AppendAllText` to handle concurrent writers (SYSTEM orchestrator + interactive user monitor).**
**Never redirect output with `*>>` in launcher scripts — PS 5.1 writes UTF-16LE. Use `*>&1 | Out-File -Append -Encoding UTF8` instead.**

---

## Manifest and integrity

`manifest.json` and `manifest.sig` are generated by `ci.yml` on every push to main. The manifest job only runs after lint and validate-tasks both pass.

Signing uses GPG asymmetric keys. See `docs/gpg-setup.md`. GitHub Secrets needed:
- `GPG_PRIVATE_KEY` — full `-----BEGIN PGP PRIVATE KEY BLOCK-----` block
- `GPG_PASSPHRASE` — key passphrase

Paste the public key into `install.ps1` at `$GPG_PUBLIC_KEY`. **Currently configured** — GPG signing is active and producing real signatures.

---

## CI workflow (`ci.yml`)

Three jobs run in order on every push to main:

1. **lint** — PSScriptAnalyzer + custom checks: PS51 compat, task XML, hardcoded paths, stage contracts, state property casing, `Add-Content` ban (`IO-BannedAddContent`), inline-if-as-argument ban (`PS51-InlineIfArgument`)
2. **validate-tasks** — actually registers and unregisters each task definition against the Windows scheduler
3. **sign manifest** — only runs if both above pass; generates and GPG-signs manifest.json

Bot commits (`chore: update manifest`) skip lint and validate — no code changed.
Manifest job skips on PRs — only runs on merged pushes.

---

## Common failure points

| Symptom | Cause | Status |
|---|---|---|
| `$Window cannot be retrieved` | XAML parse failed before assignment | Use `try/catch` around `XamlReader.Load`; check `monitor_crash.txt` |
| `parameter set cannot be resolved` | `-LogonType Interactive` with `-GroupId` | Omit `-LogonType` for group-based principals |
| `EndBoundary` XML error in task | `-RepetitionInterval` without `-RepetitionDuration` | Add `-RepetitionDuration (New-TimeSpan -Days 9999)` |
| `stream was not readable` | `Add-Content` with concurrent writer | **Fixed:** `Add-Content` banned, `AppendAllText` everywhere |
| Wide-character/UTF-16 logs | `*>>` redirect in PS 5.1 | **Fixed:** launchers use `Out-File -Encoding UTF8` |
| `$WD cannot be retrieved` | `Export-ModuleMember -Variable` unreliable | **Fixed:** Config.psm1 exports `Get-WDConfig` function |
| Orchestrator crash on first run | Bootstrap ran inline as user, not SYSTEM | **Fixed:** bootstrap triggers WinDeploy-Resume task |
| State case mismatch | camelCase vs PascalCase | **Fixed:** lint rule `State-CamelCase` enforces PascalCase |
| `winget not found` under SYSTEM | winget not on SYSTEM PATH | **Fixed:** resolves full path from WindowsApps |
| BrushConverter crash in Monitor | `switch` returned Object[] to brush converter | **Fixed:** `New-Brush` guards against array input |
| `DisplayName` crash in AppInstall | StrictMode + missing registry property | **Fixed:** guard with `PSObject.Properties['DisplayName']` |
